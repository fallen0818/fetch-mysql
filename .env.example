# Database
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=0818
DB_NAME=dbpanelco

# Server
PORT=3000

# CORS — set to your front-end origin in production, e.g. https://yourdomain.com
CORS_ORIGIN=*

# Set to "production" in prod to hide error details from clients
NODE_ENV=development
